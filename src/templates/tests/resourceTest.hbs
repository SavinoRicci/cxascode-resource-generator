package {{snake packageName}}

import (
	"terraform-provider-genesyscloud/genesyscloud/provider"
	"terraform-provider-genesyscloud/genesyscloud/util"
	"testing"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/resource"

	"github.com/hashicorp/terraform-plugin-sdk/v2/terraform"
)

/*
The resource_genesyscloud_{{snake packageName}}_test.go contains all of the test cases for running the resource
tests for {{snake packageName}}.
*/

func TestAccResource{{pascal packageName}}(t *testing.T) {
	t.Parallel()
	var ()

	resource.Test(t, resource.TestCase{
		PreCheck:          func() { util.TestAccPreCheck(t) },
		ProviderFactories: provider.GetProviderFactories(providerResources, providerDataSources),
		Steps: []resource.TestStep{},
		CheckDestroy: testVerify{{pascal packageName}}Destroyed,
	})
}

func testVerify{{pascal packageName}}Destroyed(state *terraform.State) error {
	return nil
}
